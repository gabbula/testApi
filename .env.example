# Environment variables (copy to .env and customize)
NODE_ENV=development
PORT=3000

# Database (if needed)
# DATABASE_URL=postgresql://user:password@localhost:5432/testapi

# Redis (if needed)
# REDIS_URL=redis://localhost:6379

# JWT (if needed)
# JWT_SECRET=your-super-secure-jwt-secret-key

# API Configuration
API_VERSION=v1
API_RATE_LIMIT=100

# Logging
LOG_LEVEL=info